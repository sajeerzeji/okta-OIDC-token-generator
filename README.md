# Angular - OKTA OIDC Token Generator

## Configuration

To run this project, we need to change OKTA configurations in the ``api/config.json``

## Run the project by

```shell
npm install
npm start
```
